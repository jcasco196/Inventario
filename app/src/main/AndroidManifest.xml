<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="jcasco.apps.inventario">

    <uses-permission android:name="android.permission.INTERNET" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="jcasco.apps.inventario.InventarioEscogidoItem"></activity>
        <activity android:name="jcasco.apps.inventario.CrearInventario" />
        <activity
            android:name="jcasco.apps.inventario.MisInventarios"
            android:parentActivityName="jcasco.apps.inventario.Perfil" />
        <activity
            android:name="jcasco.apps.inventario.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name="jcasco.apps.inventario.EditarPerfil" />
        <activity android:name="jcasco.apps.inventario.Perfil" />
        <activity android:name="jcasco.apps.inventario.Registro" />
        <activity android:name="jcasco.apps.inventario.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>